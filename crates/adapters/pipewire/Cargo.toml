[package]
name = "venturi-pipewire-adapter"
version = "0.1.0"
edition = "2024"

[lib]
name = "venturi_pipewire_adapter"
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["sync"] }
venturi-application = { path = "../../application" }
