[package]
name = "venturi-application"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["sync", "macros", "rt-multi-thread"] }
venturi-domain = { path = "../domain" }
